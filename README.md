# Insight

TeamOptix Insight platform foundation.

## Structure

- apps/web — primary web app
- packages/ui — shared UI components
- packages/config — shared config
- packages/types — shared types
- docs — product, architecture, and decision records

