import TeamOptixShell from "@/features/teamoptix/navigation/TeamOptixShell";
import { getSupabaseServerClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function Page(){const db=await getSupabaseServerClient();const {data,error}=await db.from("workspace_requests_v").select("id,company_name,owner_name,email,phone,status,created_at").order("created_at",{ascending:false});if(error)throw new Error(error.message);return <TeamOptixShell><main className="workspace-shell"><section className="workspace-main"><header className="automation-domain-header"><span className="workspace-eyebrow">TeamOptix · Platform</span><h1>Workspace Requests</h1><p>Persisted public intake submissions.</p></header><div className="platform-config-list">{data?.map(x=><article className="workspace-card" key={x.id}><p className="workspace-eyebrow">{x.status} · {new Date(x.created_at).toLocaleString()}</p><h2>{x.company_name}</h2><p>{x.owner_name} · {x.email}{x.phone?` · ${x.phone}`:""}</p></article>)}</div></section></main></TeamOptixShell>}
