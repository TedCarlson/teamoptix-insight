/*
NEXT PASS

Move the entire derived useMemo chain from DispatchPage into this file.

This module intentionally exists first so the extraction can happen
incrementally with zero behavioural change.

Target API:

export function buildDispatchWorkspaceModel(...)
*/
